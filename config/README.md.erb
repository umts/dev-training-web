UMTS Developer Training
=======================

Progress for <%= user_name %>
-----------------------------

* [Milestone progress][milestones]
* [Remaining issues][issues]

Once training is complete, this repository can be archived or deleted on the
[Settins page][settings]. Or, you can hold on to it
as a cherished keepsake.

---

This training repository was initialized on
<%= Time.now.strftime('%Y-%d-%d at %R') %> using UMass Transportation IT's
[`dev-training-web`][dev-training-web] tool.

[milestones]: <%= repo_relative('/milestones') %>
[issues]: <%= repo_relative('/issues') %>
[settings]: <%= repo_relative('/settings') %>
[dev-training-web]: https://github.com/umts/dev-training-web
